
```scss
:host {
  all: initial;
}

* {
  color: inherit;
  font: inherit;
  box-sizing: inherit;
}

#counter {
  display: flex;
  width: (48px * 3) + 24px;
  padding: 12px;
  border-radius: 12px;
  color: #fff;
  font: 16px/1 'Inter', sans-serif;
  box-sizing: border-box;
}

button, .count {
  width: 48px;
  height: 36px;
}

button {
  border: initial;
  border-radius: 6px;
  color: #fff;
  background: rgb(var(--red)) center / 24px no-repeat;
  cursor: pointer;

  &.-minus {
    background-image: tabler('circle-minus', 2, #fff);
  }

  &.-plus {
    background-image: tabler('circle-plus', 2, #fff);
  }
}

.count {
  display: flex;
  place-content: center;
  place-items: center;
  font-size: 24px;
}
```
